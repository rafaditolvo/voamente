import React from 'react';

const Separator = () => {
  return (
    <hr
      style={{
        border: 'none',
        borderBottom: '1px solid #ccc', // Estilo da linha
        margin: '16px 0', // Margem superior e inferior
      }}
    />
  );
};

export default Separator;
